jshint - все прошло гладко, проблемы были связаны только с ";", но как таковой ошибкой это не является, и с типом данных const, 
но это связано уже с этим инструментом.

jslint работает в веб-браузере, показывает бредовые ошибки, как заменить ' на ", я в 1-м файле поменяла, в другом не стала. 
Так же ему не нравяться методы определенных модулей, с этим я тоже сделать ничего не могу, сюда же входит for of, то ему for не нравится, 
то let, который используется в for. Не приятная программа. Все ошибки бредовые.

eslint ругается на переменную __dir, которую использую в коде. Аналогично убрать я ее тоже не могу.

deepScan ничего не показал.